# 生产构建：在 teacher-admin 目录复制为 .env.production 后再执行 npm run build
# （本仓库 .gitignore 会忽略 .env.production，勿把含密钥内容提交 Git）

# 必须与用户浏览器访问教师端的协议+主机一致（带 www 则四处都带 www），否则前端无法请求 API
VITE_API_BASE_URL=https://www.quizwiz.cn
