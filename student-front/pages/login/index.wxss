.login-page {
  padding-top: 16rpx;
}

.pad-card {
  padding: 28rpx 28rpx;
}

.hero-block {
  margin-bottom: 40rpx;
}

.hero-title {
  display: block;
  font-size: 52rpx;
  font-weight: 700;
  letter-spacing: -0.03em;
  line-height: 1.15;
}

.hero-lead {
  display: block;
  margin-top: 16rpx;
  font-size: 28rpx;
  line-height: 1.45;
}

.field-row {
  display: flex;
  align-items: center;
  padding: 28rpx 28rpx;
  min-height: 100rpx;
  box-sizing: border-box;
}

.field-row-single {
  padding-top: 32rpx;
  padding-bottom: 32rpx;
}

.field-label {
  width: 140rpx;
  flex-shrink: 0;
  font-size: 30rpx;
  font-weight: 500;
  color: var(--ios-label);
}

.field-input {
  flex: 1;
  min-width: 0;
  font-size: 32rpx;
  color: var(--ios-label);
}

.field-input-full {
  width: 100%;
}

.ph {
  color: var(--ios-tertiary);
}

.login-action {
  margin-top: 40rpx;
  width: 100%;
  box-sizing: border-box;
  border: none;
  font-size: 32rpx;
  font-weight: 600;
  line-height: 1.2;
}

.login-action::after {
  border: none;
}
